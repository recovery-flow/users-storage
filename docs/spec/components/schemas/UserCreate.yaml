type: object
required:
  - data
properties:
  data:
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ user_create ]
      attributes:
        type: object
        required:
          - username
        properties:
          username:
            type: string
            description: "Username"

